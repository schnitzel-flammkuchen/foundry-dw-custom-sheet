{{!-- Moves tab --}}
<div class="tab moves active" data-group="primary" data-tab="moves">
    
    <section class="notifications">
        {{#if system.isToken}}
        <div class="cell__title warning small-text">
            {{localize "DWCS.Notifications.NotLinkedToken" item=(localize "DW.Moves")}}
        </div>
        {{/if}}
    </section>

    {{!-- More custom resources (dropdown) --}}
    <section class="extra-custom-resources">
        {{!-- Toggle button on NAVIGATION TABS --}}
        {{!-- Extra custom resources --}}
        <div data-content>
            {{#each customResources}}
            <div class="cell cell--{{this.key}}">
                {{#if this.removable}}
                {{!-- Delete button --}}
                <button type="button" class="resource-control remove-extra-resource" data-resource="{{this.key}}">
                    <i class="fas fa-trash"></i>
                </button>
                {{/if}}
                <label class="cell__title">
                    <input type="text" name="system.attributes.{{this.key}}.label" value="{{this.label}}" data-dtype="String" placeholder="{{localize "DWCS.Custom.DefaultResource"}}" data-required="true">
                </label>
                <div class="cell__counter">
                    {{!-- Current value --}}
                    <input type="number" name="system.attributes.{{this.key}}.value" value="{{this.value}}" data-dtype="Number" data-stepper data-min="0" data-wrapper=".cell__counter" data-max=".{{this.key}}-max">
                    <span> / </span>
                    {{!-- Max value --}}
                    <input type="number" class="{{this.key}}-max" name="system.attributes.{{this.key}}.max" value="{{this.max}}" data-dtype="Number" data-stepper data-min="0">
                </div>
            </div>
            {{/each}}
            {{!-- BUTTON: Add extra resource --}}
            {{#if canAddResource}}
            <div class="cell cell--add-resource">
                <button type="button" class="resource-control add-extra-resource">
                    <i class="fas fa-plus"></i>
                </button>
            </div>
            {{/if}}
        </div>
    </section>

    {{#each moveCategories as |category|}}
    <div class="cell cell--{{category.name}}" data-dropdown>
        {{!-- Header (clickable) --}}
        <h2 class="cell__title" data-header>
            <label>
                {{localize category.title}}
                <i class="fas fa-chevron-up"></i>
            </label>

            <div class="item-controls" data-ignore>
                <button type="button" class="item-control item-create" title="{{localize "DW.MoveCreate"}}" data-type="move" data-moveType="{{category.moveType}}">
                    <i class="fas fa-plus"></i>
                </button>
            </div>
        </h2>

        {{!-- Dropdown content --}}
        <ol class="items-list" data-content>
            {{#each category.moves as |item|}}
            <li class="item flexrow" data-item-id="{{item._id}}" draggable="true">
                {{> "systems/dungeonworld/templates/parts/sheet-moves.html" item=item rollData=../rollData statSettings=../../system.statSettings}}
            </li>
            {{/each}}
        </ol>

    </div>
    {{/each}}

</div>